import React from 'react';
import { View, Text } from 'react-native';

const Section = (props) => {

    return(
        <View>
            {props.children}

        </View>
    )

}


export { Section };

